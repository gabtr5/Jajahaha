body {
    background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

/* Container central estilo app */
.app-container {
    background-color: #0f0f0f;
    border-radius: 25px;
    width: 90%;
    max-width: 320px; /* menor para ficar proporcional */
    padding: 20px;
    box-shadow: 0 0 30px rgba(255,0,0,0.5);
    text-align: center;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%,100%{box-shadow:0 0 20px rgba(255,0,0,0.5);}
    50%{box-shadow:0 0 35px rgba(255,0,0,0.8);}
}

h1 {
    color: #ff0000;
    font-size: 1.8em;
    text-shadow:0 0 10px #ff0000;
    margin-bottom: 20px;
    animation: glowText 2s infinite;
}

@keyframes glowText {
    0%,100%{text-shadow:0 0 10px #ff0000;}
    50%{text-shadow:0 0 20px #ff0000,0 0 30px #ff5555;}
}

button {
    width: 100%;
    padding: 15px;
    margin: 10px 0;
    font-size: 1em;
    border-radius: 50px;
    border: none;
    background-color: #ff0000;
    color: #fff;
    cursor: pointer;
    box-shadow: 0 0 10px #ff0000;
    animation: neonGlow 2s infinite;
    transition: all 0.3s ease;
}

button:hover {
    transform: scale(1.05);
    box-shadow:0 0 25px #ff0000,0 0 35px #ff5555;
}

@keyframes neonGlow {
    0%,100%{box-shadow:0 0 10px #ff0000;}
    50%{box-shadow:0 0 20px #ff0000,0 0 30px #ff5555;}
}

.links {
    display: none;
    flex-direction: column;
    gap: 12px;
    margin-top: 10px;
}

.link-bola {
    width: 100%;
    padding: 12px;
    background-color: #ff0000;
    border-radius: 50px;
    text-align: center;
    color: #fff;
    font-weight: bold;
    text-decoration: none;
    box-shadow: 0 0 8px #ff0000,0 0 15px #880000 inset;
    opacity:0;
    transform: translateY(-20px) scale(0.9);
}

.link-bola.aparecendo {
    animation: linkEntry 0.5s forwards;
}

@keyframes linkEntry {
    0% {opacity:0; transform:translateY(-20px) scale(0.9);}
    100% {opacity:1; transform:translateY(0) scale(1);}
}

.link-bola:hover {
    transform: scale(1.05);
    box-shadow:0 0 20px #ff0000,0 0 30px #ff5555 inset;
}

.assinatura {
    margin-top: 15px;
    font-size: 0.9em;
    color: #ff0000;
    text-shadow: 0 0 5px #ff0000;
    animation: glowText 2s infinite;
}

/* Modal confirmação */
.confirm-overlay {
    position: fixed;
    top:0; left:0; width:100%; height:100%;
    background: rgba(0,0,0,0.95);
    display:none;
    justify-content:center;
    align-items:center;
    z-index:10;
}

.confirm-box {
    background-color: #0f0f0f;
    border-radius: 25px;
    padding: 20px;
    width: 80%;
    max-width: 280px;
    text-align: center;
    box-shadow: 0 0 20px rgba(255,0,0,0.5);
    animation: slideUp 0.5s forwards;
}

@keyframes slideUp {
    0% {opacity:0; transform:translateY(50px);}
    100% {opacity:1; transform:translateY(0);}
}

.confirm-box h2 {
    color:#ff0000;
    margin-bottom:20px;
    font-size:1.2em;
    text-shadow:0 0 10px #ff0000;
    animation: glowText 2s infinite;
}

.confirm-box button {
    width: 45%;
    margin:5px 2%;
    padding:10px;
    border-radius:50px;
    font-weight:bold;
    animation: neonGlow 2s infinite;
}